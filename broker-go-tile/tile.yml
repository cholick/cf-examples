---
# The high-level description of your tile.
# Replace these properties with real values.
#
name: key-value-tile # By convention lowercase with dashes
icon_file: resources/icon.png
label: Simple key-value store
description: Simple key-value store with a few implementations, some of which don't actually make much sense

# Global defaults (all optional)
#
org: kv-tile-org
org_quota: 4096
space: kv-tile-space
apply_open_security_group: true

# Didn't successfully specify credentials, instead just flipped to using env values:
#   SECURITY_USER_NAME, SECURITY_USER_PASSWORD
#security.user: user
#security.password: correct-horse-battery-staple
#security:
#  user: user
#  password: correct-horse-battery-staple

properties:
- name: author
  type: string
  label: Author
  value: Matt Cholick

# Specify the packages to be included in your tile.
# The format of the section to include depends on the type
# of package you are describing. For fragments of each type
# that you can copy & paste, see:
packages:

# https://github.com/cf-platform-eng/tile-generator/blob/master/README.md
#
#packages:
#- name: broker-go
#  type: app
#  manifest:
#    path: resources/broker-go.zip
#    command: broker-go
#    memory: 16M
#    instances: 1
#    buildpack: go_buildpack


#- name: my-application
#  type: app
#  manifest:
#   path: resources/my-application-1.0.0.jar
#   command: python app.py
#   memory: 256M

- name: go-broker
  type: app-broker
  manifest:
    path: resources/broker-go.zip
    command: broker-go
    memory: 16M
    instances: 1
    buildpack: go_buildpack

# Include stemcell criteria if you don't want to accept the default.
# Since this stemcell is only used to run pre and post errands, we
# strongly recommend you leave this alone so that your tile always
# runs with the latest stemcell.
#
# stemcell_criteria:
#   os: ubuntu-trusty
#   requires_cpi: false
#   version: '3062'

# Add properties you want to pass to your applications.
# Properties specified here will not be configurable by the user.
#
#properties:
# - name: example_property
#   type: string
#   default: specify a value
#   label: Label for the field on the GUI
#   description: Longer description of the field's purpose

# Uncomment this section if you want to display forms with configurable
# properties in Ops Manager. These properties will be passed to your
# applications as environment variables. You can also refer to them
# elsewhere in this template by using:
#     (( .properties.<property-name> ))
#
#forms:
# - name: Config
#   label: Config
#   description: config_properties
#   properties:
#   - name: buildpack_rank
#     type: string
#     label: Example property
#     description: Example property

# Add any dependencies your tile has on other installed products.
# This is often appropriate when using automatic service provisioning
# for any of your packages above, with services provided by other
# products.
#
# requires_product_versions:
# - name: p-mysql
#   version: '~> 1.7'
